/*
 * Configuration file for a XIOSim multiprogrammed benchmark run.
 *
 * How to define a program:
 *
 * program PROGRAM_NAME {
 *   exec_path = "path/to/executable"
 *   command_line_args = "relevant arguments for this program".
 *   instances = number of identical instances to run.
 * }
 *
*/ Author: Sam Xi

// Global arguments for Pin and Zesto.
pinhome = "/home/skanev/pin/2.12"
pin = "pin.sh"
pintool = "./obj-ia32/feeder_zesto.so"
zesto_cfg = "../config/A.cfg"
memory_cfg = "../dram-config/DDR3-1600-9-9-9.cfg"
max = 1000000  // What is this used for?
library_path = ${LD_LIBRARY_PATH:/home/skanev/lib}
num_cores = 2

program fib {
  exec_path = "../tests/fib"
  command_line_args = ""
  instances = 1
}

program fib2 {
  exec_path = "../tests/fib"
  command_line_args = ""
  instances = 1
}

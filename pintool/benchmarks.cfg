/*
 * Configuration file for a XIOSim multiprogrammed benchmark run.
 *
 * How to define a program:
 *
 * program PROGRAM_NAME {
 *   exec_path = "path/to/executable"
 *   command_line_args = "relevant arguments for this program".
 *   instances = number of instances of this benchmark to run.
 * }
 *
 * Author: Sam Xi
*/

program fib {
  exec_path = "../tests/fib"
  command_line_args = ""
  instances = 2
}

program fib2 {
  exec_path = "../tests/fib"
  command_line_args = ""
  instances = 1
}
